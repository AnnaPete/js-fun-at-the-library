function createTitle(partialTitle) {
  return "The " + partialTitle;

}

module.exports = {
  createTitle,
  // buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}
